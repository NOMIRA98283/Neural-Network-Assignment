(Problem Set 2)

Overview:

In this part of the assignment, I used a machine learning model to analyze a dataset and make predictions. The goal was to understand how a different algorithm works and compare results.


Dataset:

The dataset contains different features which are used to predict a target value. It includes both numerical and categorical data.


What I Did:

* Loaded the dataset
* Cleaned the data (handled missing values if any)
* Converted categorical values using encoding
* Scaled the data using StandardScaler
* Split the dataset into training and testing sets

Model Used:

I used **Logistic Regression** for this problem.

It is mainly used for classification problems and predicts the probability of a class.


Result:

The model was tested on test data.

* Accuracy was used to measure performance
* Confusion matrix and classification report were also checked

The model gave a reasonable result but can be improved further.


Tools Used :

* Python
* Pandas
* NumPy
* Matplotlib
* Scikit-learn


Conclusion:

This part helped me understand how Logistic Regression works and how preprocessing affects the model performance. I also learned how to evaluate a model using different metrics.



